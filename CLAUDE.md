# LLM Wiki
AI 에이전트에 의해 관리되는 개인 지식 기반 위키
[LLM wiki pattern of Andrej Karpathy](https://gist.github.com/karpathy/442a6bf555914893e9891c11519de94f) 에 기반하고 있음

## 목적
프로그래밍과 관련된 구조화되고 상호연관된 지식 기반 위키 구축
AI 에이전트가 위키를 관리하고 사용자는 원천(sources)을 큐레이션하고 질문하며 분석을 위한 가이드라인 제공

## 구조
```
raw/
wiki/
  index.md
logs/
  YYYY-MM/
    log.md
```
- `raw/` - 불변 진실원으로 변경 불가
- `wiki/` - 위키 문서
	- `index.md` - 위키 카탈로그
- `logs/YYYY-MM/log.md` - 위키에 대한 prepend-only 변경 이력. 월별로 디렉터리를 나눠 보관하며, 현재 월 디렉터리가 없으면 새로 생성
- 필요한 경우 새로운 카테고리에 대한 `wiki/`의 서브디렉터리 생성


## 위키
- `wiki/` 경로에 작성
- `raw/`를 기본 진실 공급원으로 사용
- 모든 내용은 반드시 `raw/` 내 문서나 웹 상의 문서에 기반해야 함
- 다음의 경우 각주(`[^n]`)로 표시하며 판단 근거를 기술
	- 출처 간 불일치 또는 모순 존재
	- 사실이 아닌 추론이나 주장
- 각주 내용은 문서 최하단에 작성
- 생성, 수정, 삭제 시
	- Write/Edit 실행 전 변경 내용(요약 또는 diff)을 먼저 제시하고 사용자의 명시적 승인을 받은 후에만 실행
		- 수정을 요청하는 메시지 자체는 승인으로 간주하지 않음
		- 조사(raw 확인, 관련 문서 탐색 등)는 승인 없이 자유롭게 진행 가능
	- 수행 후 현재 월의 `logs/YYYY-MM/log.md`에 이력 추가(해당 월 디렉터리가 없으면 생성)
	- 상단 메타데이터의 `updated` 항목 수정
		- `bash`의 `date` 사용 (YYYY-MM-DD HH:MM:SS)
- `wiki/index.md`에서 각 페이지에 대한 서술은 주제 수준에서 간략히 `[[페이지명]] — 주제` 형식으로 작성
- 신규 문서 작성 시 기존 문서 스타일 준수
- 문서 제목은 간단하게 작성
## 형식
모든 위키 페이지는 아래 형식 준수:
```
---
title: 페이지 제목
updated: 최종 업데이트 일시 (YYYY-MM-DD HH:MM:SS)
tags:
  - tag
---
본문. 간결하고 명확한 제목과 문장 사용.
관련 내용 또는 개념은 [[wiki-links]]를 이용해 링크.
본문 각 항목은 번호 (e.g. 1. 개요 1.1 상세)

---
## Sources
- 출처가 되는 `raw/` 내 파일 또는 웹 페이지 링크

---
## Related pages
- [[related-wiki]]
```

## 이력
`logs/YYYY-MM/log.md`(작업 시점 기준 현재 월)에 이력 기록은 다음을 따름:
- 모든 위키 페이지 작성, 수정, 삭제 시 이뤄짐
- 내용은 짧고 간결해야 함
	- 수정된 내용 전체가 아닌 짧은 개요만 작성
- 메타데이터 수정에 대한 이력은 불필요함
- 새로운 이력은 해당 월 파일의 최상단에 기록
- 월이 바뀌면 새 `logs/YYYY-MM/` 디렉터리와 `log.md`(`# log\n\n---\n\n` 헤더 포함)를 새로 생성해 기록 시작

## 권고사항
- 수식에는 LaTeX를 적극 사용
- 최소한의 실행 가능한 코드 예시 포함
- 개발, 특히 백엔드 중심 관점 유지
- 수사적 표현·과장을 배제하고 기술적 사실에 집중한다.
    - 정확하고 객관적인 어조를 유지하고 기술적 데이터와 정확성에 집중
- 최대한 간결해야 함 
	- 제목과 목차는 서술형 또는 수식어 지양하여 짧은 단어
	- `()`는 축약어나 다른 언어 -> 한글 번역명 또는 한글 -> 다른 언어 번역, 혹은 링크를 위해서만 사용
	- 설명 또는 정보성 서술은 본문에 위치
- 불필요한 줄바꿈 지양
	- 맥락 또는 중심 내용이 달라지는 경우에만 문단 분리
- 두 구·절을 대시(-, —)로 이어붙이는 연결 표현 금지
	- 자연스러운 한 문장으로 잇거나 문장을 분리해서 작성