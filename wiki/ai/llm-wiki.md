---
title: LLM Wiki 패턴
updated: 2026-07-08 10:32:15
tags:
  - ai
  - knowledge-management
  - llm
---

## 1. 개요

LLM Wiki는 Andrej Karpathy가 제안한 개인 지식 관리 패턴이다. LLM을 단순 질의응답 도구가 아니라 **지식 베이스를 구축·유지하는 에이전트**로 활용한다.

핵심 통찰: *"위키 유지의 지루한 부분은 읽거나 생각하는 게 아니라 북키핑이다."* 참조 갱신, 크로스링크 유지, 모순 탐지 같은 반복 작업을 LLM이 담당하고, 인간은 소스 투입과 방향 큐레이션에 집중한다.

## 2. RAG와의 차이

기존 RAG는 쿼리마다 소스를 재탐색·재합성한다. LLM Wiki는 소스를 한 번 읽어 위키로 **컴파일**하고, 이후 새 소스가 추가될 때만 증분 갱신한다.

| 구분 | RAG | LLM Wiki |
| :--- | :--- | :--- |
| 지식 처리 | 쿼리마다 소스 재탐색 | 한 번 컴파일, 이후 증분 갱신 |
| 지식 누적 | 없음 | 페이지가 쌓이며 심화 |
| 북키핑 | 없음 | LLM이 담당 |
| 적합한 용도 | 일회성 탐색 | 장기 도메인 지식 관리 |

## 3. 구조

세 레이어로 구성된다.

```
raw/        -- 불변 소스 (LLM이 읽되 수정 금지)
wiki/       -- LLM이 생성·관리하는 구조화 문서
schema      -- 위키 형식, 워크플로우, 규칙 정의 (CLAUDE.md, AGENTS.md 등)
```

- **`raw/`**: 기사, 논문, 공식 문서 등 원본 소스. 합성의 근거로만 사용하며 불변을 원칙으로 한다.
- **`wiki/`**: `[[wiki-link]]`로 상호 연결된 마크다운 페이지. 인간이 직접 수정하지 않고 LLM이 관리한다.
- **스키마([[ai-agent-schema]])**: LLM 에이전트가 따르는 위키 형식과 워크플로우 규칙. 에이전트 종류(Claude Code, Codex, Gemini CLI 등)에 따라 파일명이 다르다. **스키마가 실질적인 제품**이며, 이것이 일반 LLM을 규율 있는 지식 관리자로 만든다.

## 4. 워크플로우

### 4.1. ingest
새 소스를 읽어 위키에 통합한다. 소스 1개가 보통 10~15개 페이지에 영향을 준다.
1. `raw/`의 소스 파일 및 웹 문서를 읽음
2. 관련 위키 페이지를 갱신하고 크로스링크 유지
3. 변경 이력을 로그에 기록

### 4.2. query
위키를 기반으로 질문에 답한다.
1. 인덱스(index.md)에서 관련 페이지 식별
2. 해당 페이지를 읽어 `[[페이지]]` 인라인 인용으로 답변 생성
3. 유의미한 답변은 신규 위키 페이지로 저장 가능

### 4.3. lint
위키 품질을 주기적으로 감사한다.
- **깨진 링크**: `[[wiki-link]]` 대상 미존재 또는 웹 URL 접근 불가
- **모순**: 소스 또는 위키 페이지 간 정보 불일치
- **고아 페이지**: 다른 페이지에서 참조되지 않는 페이지

## 5. 확장 패턴 (LLM Wiki v2)

원본 패턴이 규모에서 한계를 보이자 여러 개선이 제안됐다.

- **신뢰도 점수(Confidence Scoring)**: 각 사실에 소스 수·최신성·모순 여부 메타데이터를 부여
- **지식 버전화**: 새 정보가 기존 정보를 명시적으로 대체하고 이력을 보존
- **지식 그래프**: 페이지 간 링크에 관계 의미(uses, depends-on, contradicts 등)를 부여
- **이벤트 드리븐 자동화**: 새 소스 추가 시 자동 ingest, 세션 시작 시 컨텍스트 로딩

## 6. 구현 예시

여러 에이전트(Claude Code, Codex, Gemini CLI)와 결합한 오픈소스 구현이 GitHub에 다수 존재한다. 공통 특징은 에이전트가 읽는 스키마 파일(CLAUDE.md, AGENTS.md, GEMINI.md 등)로 동작을 제어하고, 로컬 마크다운 파일만으로 서버 없이 구동된다는 점이다.

---

## Sources
- [llm-wiki (Andrej Karpathy)](https://gist.github.com/karpathy/442a6bf555914893e9891c11519de94f)
- [SamurAIGPT/llm-wiki-agent](https://github.com/SamurAIGPT/llm-wiki-agent)
- [LLM Wiki v2 — rohitg00](https://gist.github.com/rohitg00/2067ab416f7bbe447c1977edaaa681e2)

---

## Related pages
- [[ai-agent-schema]]
- [[okf]]
