# wiki-lint report — 2026-08-11

- **범위**: `wiki/` 전체 (200개 문서, 위키링크 1,265건 · 고유 대상 208종 · 웹 URL 743종 고유)
- **결과**: broken links 5건(전부 웹 링크) / conflicts 0건(스팟체크) / 기타 정합성 지적 3건
- **조치**: 지적 사항 전부 수정 완료 (npm/npx 페이지 신규 작성은 별도 지시 대기)

---

## 1. 위키링크 — 실제 깨짐 0건

고유 대상 208종 전부 대응 파일 존재. `index.md` 미등재 페이지 0건, `index.md`에만 있고 파일 없는 항목 0건.

**오탐 9종** — 문법 예시 또는 코드 블록 내 대괄호:

| 위치 | 패턴 | 성격 |
|---|---|---|
| `wiki/linux/system/environment-variables.md` L57, L303, L308 | `[[ -z "$JAVA_HOME" ]]` 등 | bash 조건식 |
| `wiki/linux/text/sed.md` L121 | `[[:space:]]` | POSIX 문자 클래스 |
| `wiki/ai/llm-wiki.md` L38·L52·L57, `ai-agent-skill.md` L200, `ai-agent-schema.md` L84·L110·L121 | `[[wiki-link]]`, `[[wikilinks]]`, `[[페이지]]` | 위키링크 문법 설명 예시 |

전회(2026-07-21) 지적된 `[[spring-event-error-handling]]` 미존재 건은 해소됨(문서 생성 확인).

---

## 2. 웹 링크 — 실제 깨짐 5건 → 전부 조치

708개 URL을 리다이렉트 추종·브라우저 UA로 검사. **628건 200 / 70건 403 / 10건 404**.

### 2.1 404 중 실제 깨진 링크 5건 (전부 Sources 섹션)

전회(2026-07-21)에서 지적된 5건이 미조치 상태로 잔존했다. 이번 회차에서 대체 URL로 교체하고, 대체 불가 1건은 제거했다.

| 파일 | 깨진 URL | 조치 |
|---|---|---|
| `wiki/dbms/execution-plan.md` | `docs.oracle.com/.../19/tgsql/reading-execution-plans.html` | → `.../19/tgsql/query-execution-plans.html` (200 확인). 링크 제목도 "Query Execution Plans"로 수정 |
| `wiki/programming/cqrs.md` | `docs.axoniq.io/axon-framework-reference/` | → `https://docs.axoniq.io/reference-guide/` (200 확인) |
| `wiki/web/ssr-vs-csr.md` | `vercel.com/blog/how-to-choose-the-best-rendering-strategy` | → `.../how-to-choose-the-best-rendering-strategy-for-your-app` (200 확인) |
| `wiki/java/crypto/cmp-bouncycastle.md`<br>`wiki/java/crypto/cert-path-validation.md` | `bouncycastle.org/docs/pkixdocs1.8on/index.html` | → `https://javadoc.io/doc/org.bouncycastle/bcpkix-jdk18on/latest/index.html` (200 확인). BouncyCastle 사이트 개편으로 구 javadoc 경로 소멸 |
| `wiki/java/common/java-process-analysis-tools.md` | `honeymon.io/tech/2019/05/30/java-memory-leak-analysis.html` | **제거**. http/https 모두 404, 글 자체 소실. archive.org는 429로 확인 실패. 동일 주제 출처(dev.java, voidmainvoid, homoefficio)가 남아 내용 근거는 유지됨 |

### 2.2 404 오탐 5종 (조치 불필요)

| URL | 성격 |
|---|---|
| `http://maven.apache.org/POM/4.0.0` (`maven.md` L33·48·66) | pom.xml XML 네임스페이스 URI. 링크 아님 |
| `github.com/user/repo.git`, `github.com/original/repo.git`, `github.com/user/large-repo.git` (`git-clone.md` 18곳) | 명령어 예시 플레이스홀더 |
| `github.com/**` (`git-config.md` L277) | `insteadOf` 패턴 예시 |

### 2.3 403 — 검증 불가 70건

봇 차단으로 curl·WebFetch 모두 접근 거부. 브라우저에서는 정상일 가능성이 높아 깨짐으로 판정하지 않았다. 다음 회차에서도 동일하게 판정 보류될 것으로 예상된다.

`www.baeldung.com` 45 · `dev.mysql.com` 15 · `medium.com` 2 · `www.cloudflare.com` 2 · `blogs.oracle.com` / `linux.die.net` / `mvnrepository.com` / `www.oreilly.com` / `www.keysight.com` / `www.info-zip.org` 각 1

---

## 3. Conflicts — 0건 (스팟체크)

200개 문서 전수 의미 대조는 범위를 벗어나, 주제가 겹치는 군집 위주로 확인했다.

| 검사 항목 | 판정 |
|---|---|
| HikariCP 공식·기본값 — `hikari-datasource` ↔ `hikari-pool-sizing` ↔ `hikari-deadlock` | 일치 (`connectionTimeout` 30s, `(core_count × 2) + spindle`, `Tn × (Cm-1) + 1`) |
| Fat jar 서명 충돌 — `fat-jar-signature-error` ↔ `jar-signing` ↔ `gradle-application`(신규) | 일치 |
| bootJar mainClass 결정 — `gradle-task` §2.9 ↔ `gradle-application` §2.4 | 일치 (미지정 시 자동 탐지) |
| Shadow 플러그인 ID `com.gradleup.shadow` — `fat-jar-signature-error` ↔ `gradle-application` ↔ `sbom-java` | 일치 |
| JS 모듈 — `module-system` ↔ `commonjs` ↔ `es-module` (동기/비동기, top-level await, 캐시 키, 트리쉐이킹) | 일치, 각주로 추론 표시 정상 |

메타데이터 검증(frontmatter `title`/`updated`/`tags`, `## Sources`·`## Related pages` 존재, 각주 정의·참조 짝) — 200개 전부 통과. 중복 `title` 0건. 파일명 kebab-case 위반은 `openssl-s_client` 1건이나 OpenSSL 서브커맨드 실명(`s_client`)이라 정상.

---

## 4. 기타 정합성 지적 3건

### 4.1 `raw/` 미반영 소스 9건 → 4건 조치, 5건 보류

`raw/` 18개 파일 중 9개가 어느 위키 페이지에서도 인용되지 않았다.

**Sources에 `raw/` 경로 추가 (4건 — 조치 완료)**

| raw 파일 | 대응 페이지 | 조치 전 Sources |
|---|---|---|
| `java/spring/Spring Events (Baeldung).md` | `spring-event.md` | Baeldung URL조차 없음 |
| `java/spring/Redis Cluster…NHN Cloud Meetup.md` | `redis.md` | 동일 글의 웹 URL만 기재 |
| `java/아 엑셀다운로드…우아한.md` | `excel.md` | 우아한형제들 글 미기재 |
| `troubleshoot/native-lib-setup.md` | `jni.md` | "사용자 실측"으로만 표기 |

각 raw 파일의 주제가 대응 페이지와 일치함을 확인한 뒤 Sources 최상단에 경로를 추가했다.

**위키 페이지 자체가 없는 것 (5건 — 보류)**

- `raw/node/npm Docs.md`, `raw/node/Introducing npx an npm package runner.md`, `raw/node/serve.md` — Node/npm 카테고리 부재. **사용자 지시 대기 중**
- `raw/crypto/ANS.1.md` — ASN.1 전용 페이지 없음(`cms`·`x509-certificate`에서 부분 언급만). 파일명 `ANS.1`은 `ASN.1` 오타로 보이나 `raw/`는 불변이므로 보고만 함
- `raw/java/common/일급 컬렉션 (First Class Collection)의 소개와 써야할 이유.md` — `first-class-citizen.md`는 "일급 객체"를 다루며 일급 컬렉션은 미반영

### 4.2 태그 표기 흔들림 2건 → 조치 완료

- `language` — `template-literals.md`에서 제거. 같은 `wiki/javascript/`의 `truthy-falsy`·`null-operators`·`variable-declaration`이 이미 제거된 상태였으므로 카테고리 내 일관성을 맞춤. `expression-vs-statement.md`(java)·`first-class-citizen.md`(programming)는 다른 카테고리이므로 유지 — 태그 자체의 폐지 여부는 미결
- `best-practice`(단수) → `best-practices`(복수)로 통일. `good-test-practices.md`, `inverse-operation-testing.md` 2건 수정. 관행 집합을 가리키는 의미상 복수형 채택

### 4.3 Shadow 플러그인 버전 표기 불일치 → 조치 완료

- `gradle-application.md` §4.3의 `version '9.6.1'` → `version '<version>'`
- `fat-jar-signature-error.md`의 기존 플레이스홀더 표기에 맞춤. 버전 고정은 시간 경과에 따라 노후화되므로 플레이스홀더를 표준으로 채택

---

## 5. 수정 파일 목록 (14건)

| 파일 | 사유 |
|---|---|
| `wiki/dbms/execution-plan.md` | 깨진 링크 교체 (2.1) |
| `wiki/programming/cqrs.md` | 깨진 링크 교체 (2.1) |
| `wiki/web/ssr-vs-csr.md` | 깨진 링크 교체 (2.1) |
| `wiki/java/crypto/cmp-bouncycastle.md` | 깨진 링크 교체 (2.1) |
| `wiki/java/crypto/cert-path-validation.md` | 깨진 링크 교체 (2.1) |
| `wiki/java/common/java-process-analysis-tools.md` | 소실 링크 제거 (2.1) |
| `wiki/java/spring/spring-event.md` | raw 경로 추가 (4.1) |
| `wiki/java/spring/redis.md` | raw 경로 추가 (4.1) |
| `wiki/java/excel.md` | raw 경로 추가 (4.1) |
| `wiki/java/common/jni.md` | raw 경로 추가 (4.1) |
| `wiki/javascript/template-literals.md` | `language` 태그 제거 (4.2) |
| `wiki/java/test/good-test-practices.md` | 태그 복수형 통일 (4.2) |
| `wiki/java/test/inverse-operation-testing.md` | 태그 복수형 통일 (4.2) |
| `wiki/java/common/gradle-application.md` | Shadow 버전 표기 통일 (4.3) |

수정 후 재검증: 교체 URL 4종 전부 200, `honeymon` 잔존 0건, 200개 문서 메타데이터 검증 전부 통과, 신규 깨진 위키링크 0건.

---

## 6. 다음 회차 이관 항목

- `raw/node/*` 3건 기반 npm/npx 페이지 작성 (사용자 지시 대기)
- `raw/crypto/ANS.1.md` — ASN.1 페이지 작성 여부 결정
- `raw/java/common/일급 컬렉션…` — 일급 컬렉션 페이지 작성 또는 `first-class-citizen.md` 확장 여부 결정
- `language` 태그 폐지 여부 결정 (현재 `expression-vs-statement`·`first-class-citizen` 2건 사용)
- 403 차단 70건 — 브라우저 기반 확인 수단 필요
