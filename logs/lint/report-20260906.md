# wiki-lint report — 2026-09-06 (링크 점검)

- **범위**: `wiki/` 전체 (238개 문서, 위키링크 1,561건 · 고유 대상 242종 · Sources 웹 URL 855종 고유)
- **결과**: broken links 1건(위키링크) / 웹 링크 깨짐 0건 / 오탐 11종
- **조치**: 1건 수정 완료

---

## 1. 위키링크 — 실제 깨짐 1건

고유 대상 242종 중 241종은 대응 파일 존재. `index.md` 미등재 페이지 0건, `index.md`에만 있고 파일 없는 항목 0건(237/237 완전 일치).

### 1.1 실제 깨진 링크 1건 → 조치 완료

| 파일 | 링크 | 문제 | 조치 |
|---|---|---|---|
| `wiki/javascript/fetch-api.md` L12 | `[[XMLHttpRequest]]` | 대응 페이지 없음. 인접한 `[[promise\|Promise]]`·`[[async-await]]`와 동일한 방식으로 실제 교차참조 의도로 쓰임 (2026-08-31 신규 문서, 이전 회차 이후 발생) | 백틱 코드 스팬 `` `XMLHttpRequest` ``로 전환. 별도 페이지 신설은 보류 |

### 1.2 오탐 11종 (조치 불필요)

| 위치 | 패턴 | 성격 |
|---|---|---|
| `wiki/linux/system/environment-variables.md` 등 | `[[ -z "$JAVA_HOME" ]]` 등 | bash 조건식 |
| `wiki/linux/text/sed.md` | `[[:space:]]` | POSIX 문자 클래스 |
| `wiki/ai/llm-wiki.md` L38·52·57 | `` `[[wiki-link]]` ``, `` `[[페이지]]` `` | 백틱 코드 스팬 |
| `wiki/ai/ai-agent-skill.md` L200 | `[[wiki-link]]` | ` ```markdown ` 코드 펜스(L187~) 내부 — Obsidian이 링크로 파싱하지 않음 |
| `wiki/ai/ai-agent-schema.md` L84 | `[[wiki-links]]` | ` ```markdown ` 코드 펜스(L80~) 내부 — 동일 사유 |
| `wiki/ai/ai-agent-schema.md` L110·121 | `` `[[wikilinks]]` ``, `` `[[wikilink]]` `` | 백틱 코드 스팬 |

---

## 2. 웹 링크 — 실제 깨짐 0건

855개 고유 URL(Sources 섹션 기준)을 리다이렉트 추종 + 브라우저 UA로 검사. **200 OK 775건 / 403 80건 / 000·404 0건**.

이전 회차(2026-08-11)에서 교체·제거 조치된 5건 모두 재발 없이 유지.

### 2.1 403 — 80건, 깨짐 아님으로 판정

curl 접근이 전부 차단되어 `www.baeldung.com`(45) · `dev.mysql.com`(23) · `www.cloudflare.com`(2) · `blogs.oracle.com`(2) · `www.oreilly.com`/`www.npmjs.com`/`www.keysight.com`/`www.cybertec-postgresql.com`/`mvnrepository.com`/`medium.com`/`linux.die.net`/`bugs.java.com`(각 1)에서 403이 나왔다. WebFetch로 재검증한 결과 `dev.mysql.com`·`linux.die.net`·`www.keysight.com` 3종은 실제로는 정상 페이지가 로드됨을 확인했고, 나머지는 WebFetch도 동일하게 차단당해 자동화 도구로는 판정 불가였다. 봇 차단 패턴이 일관되므로 80건 전부 깨짐으로 분류하지 않았다 — 이전 회차(2026-08-11)와 동일 판정.

placeholder성 URL(`api.example.com`, `github.com/user/repo.git`, `maven.apache.org/POM/4.0.0` XML 네임스페이스 등)은 코드 예시 내 값이라 Sources 인용이 아니므로 집계에서 제외했다(전부 000/404였으나 Sources 섹션 밖).

---

## 3. 수정 파일 목록 (1건)

| 파일 | 사유 |
|---|---|
| `wiki/javascript/fetch-api.md` | 깨진 위키링크 `[[XMLHttpRequest]]` → `` `XMLHttpRequest` `` 코드 스팬 전환 (1.1) |

---

## 4. 다음 회차 이관 항목

- 403 차단 80건 — 브라우저 기반 확인 수단 필요(2026-08-11 회차부터 이관 지속 중)
- Conflicts·기타 정합성 점검은 이번 회차 범위(링크 점검) 밖 — 필요 시 별도 실행
